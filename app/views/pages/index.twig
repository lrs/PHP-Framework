{% set title = _('page.home.title') %}
{% set desc = _('page.home.description') %}

{% extends 'layouts/default.twig' %}

{% block banner %}
    {{ include('components/banner.twig', { title: title }) }}
{% endblock %}

{% block content %}
    <div class="grid__col-12 grid__col-center">
        <h2>{{ _('page.home.welcome') }} {{ name }}!</h2>

    </div>
    <div class="grid__col-12 grid__col-md-4">
        <p>{{ lorem(30) }}</p>
    </div>
    <div class="grid__col-12 grid__col-md-4">
        <p>{{ lorem(30) }}</p>
    </div>
    <div class="grid__col-12 grid__col-md-4">
        <p>{{ lorem(30) }}</p>
    </div>
{% endblock %}
