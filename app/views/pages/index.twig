{% set title = _('page.home.title') %}
{% set desc = _('page.home.description') %}

{% extends 'layouts/default.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block content %}
    <div class="grid__col-12">
        <p>{{ _('page.home.welcome') }} {{ name }}!</p>
        <p>{{ lorem(30) }}</p>
    </div>
{% endblock %}
